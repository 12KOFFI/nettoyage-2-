<form method="post" action="{{ path('app_demande_devis_delete', {'id': demande_devi.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette demande ? Cette action est irréversible.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_devi.id) }}">
    <button class="btn btn-danger">
        <i class="fas fa-trash me-2"></i>Supprimer définitivement
    </button>
</form>
