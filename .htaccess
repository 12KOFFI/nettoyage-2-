# Redirect everything from the repository root into the Symfony public/ directory
RewriteEngine On

# Avoid redirect loop when accessing files already under /public
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ public/$1 [L,QSA]
