<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si le fichier existe (asset), on le sert directement
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^ - [L]

    # Sinon, toutes les routes vont vers index.php
    RewriteRule ^ index.php [QSA,L]

</IfModule>
